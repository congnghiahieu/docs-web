import { Callout } from 'nextra/components';

## Syntax

```docker
FROM
RUN
WORKDIR
EXPOSE 80
ADD = COPY
ENTRYPOINT
CMD
```

<Callout type='warning' emoji='⚠️'>
  Tài liệu về Dockerfile sẽ tiếp tục được cập nhật
</Callout>
