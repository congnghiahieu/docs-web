## `gitk`

`gitk` là công cụ đồ họa cho phép kiểm tra lịch sử commit dưới dạng đồ họa

```bash
gitk
```

## `git gui`

`git gui` là công cụ đồ họa cho phép thực hiện các thao tác git add, git commit, git checkout, ...

```bash
git gui
```
